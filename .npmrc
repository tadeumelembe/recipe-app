# React Native / Expo require a flat node_modules layout: Metro's resolver and
# native autolinking (Gradle + CocoaPods) both walk real directories and do not
# follow pnpm's default symlinked store.
node-linker=hoisted
